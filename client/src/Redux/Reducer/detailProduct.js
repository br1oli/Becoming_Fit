/* import { GET_DETAIL_PRODUCT } from '../Actions/actionTypes';

const initialState = {
  product: {},
};

export const detailProduct = (state = initialState, { type, payload }) => {
  switch (type) {
    case GET_DETAIL_PRODUCT:
      return {
        ...state,
        product: payload,
      };
    default:
      return state;
  }
}; */
